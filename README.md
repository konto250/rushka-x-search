# X検索式ビルダー

任意の検索語と条件からXの検索式を作成する静的アプリです。検索式のコピー、X検索の起動、ブックマークレットの生成ができます。

## 使い方

1. `index.html` をブラウザで開きます。
2. 検索語を入力します。`猫 OR #猫` のようなXの検索演算子もそのまま使えます。
3. 必要な条件を選びます。検索語を空欄にして条件だけを使うこともできます。
4. 検索式をコピーするか、`Xで開く（最新）` を押します。
5. ブックマークレットを使う場合は、表示されたリンクをブックマークバーへドラッグするか、JavaScriptをコピーしてブックマークのURLへ貼り付けます。

`Xで開く（最新）` を押すと、その時点の検索条件がブラウザのローカルストレージに保存されます。検索式の履歴には新しい順に最大5件を表示し、各項目からX検索を開く、条件をフォームへ再展開する、またはその履歴だけを削除できます。同じ検索式を再度開いた場合は、その条件を先頭へ移します。

## 条件

- メディアのみ: `filter:media`
- フォロー中のユーザーのみ: `filter:follows`
- 引用リツイートを除外: `-filter:quote`
- リツイートを除外: `-filter:retweet`
- リプライを除外: `-filter:replies`
- 指定ユーザーのみ検索: `from:@user` を追加。スペース区切りで複数指定すると `(from:@user1 OR from:@user2)` の形式にします。
- ユーザーを除外: 入力した各ユーザーを `-from:@user` に変換。入力時に `@` を付けても、検索式では一つだけ付けます。
- キーワードを除外: 1行に1語句を入力します。`猫` は `-猫`、`黒い 猫` は `-"黒い 猫"` に変換します。
- 日付を指定する: カレンダー指定と「過去N日（今日を含む）」を切り替えられます。カレンダーでは開始日から終了日までドラッグするか、開始日をクリックして月を移動し、終了日をクリックします。終了日を含めるため、`until:` には翌日を設定します。過去N日ではブラウザの現地日付を基準に `since:` だけを自動計算します。

カレンダー指定では「期間をクリア」で日付条件だけを消せます。

終了日が開始日以前、または過去の日数が有効な正の整数でない場合は検索・コピーを無効にします。X検索リンクには最新表示用の `f=live` が付きます。過去日数指定のブックマークレットは実行した日付から開始日を再計算します。その他の条件を変えた後は再登録してください。

ビルドは不要です。`index.html` を直接開くか、任意の静的サーバーで配信してください。

## Vercelへデプロイ

このプロジェクトフォルダで以下を実行します。

```powershell
npx vercel login
npx vercel --prod
```

初回はVercelの案内に従ってプロジェクトを関連付けてください。
